#!/bin/bash
set -e

BASEDIR=$(dirname $0)
TEMPLATES=$BASEDIR/../../templates
source $BASEDIR/../../common

[[ -z "$1" ]] && SET=1 || SET=$1

# ------------------------------------------------------------------------------
# get for an existing schedule
# ------------------------------------------------------------------------------
schedule_id=$(jq -r ".[0].id" $TMP/api-pri-meeting-schedule-add-id-$SET-1-res)
JSON=$(cat <<EOF
{
    "id": "$schedule_id"
}
EOF
)

bash $TEMPLATES/action-member-attr "$SET" "pri" "meeting/schedule" "get" \
    "id" "$JSON" 1
