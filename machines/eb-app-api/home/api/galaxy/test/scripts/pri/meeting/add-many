#!/bin/bash
set -e

BASEDIR=$(dirname $0)
TEMPLATES=$BASEDIR/../../templates
source $BASEDIR/../../common

[[ -z "$1" ]] && NUM=1 || NUM=$1

room_id=$(jq -r ".[0].id" $TMP/api-pri-room-add-id-1)

# ------------------------------------------------------------------------------
# add many new meetings
# ------------------------------------------------------------------------------
hidden=true
for i in $(seq 1 $NUM); do
    [[ "$hidden" = false ]] && hidden=true || hidden=false

    name="meeting-$(date +'%s')-$i-$RANDOM"
    JSON=$(cat <<EOF
    {
        "room_id": "$room_id",
        "name": "$name",
        "info": "some meeting info",
        "schedule_type": "permanent",
        "schedule_attr": "{}",
        "hidden": $hidden,
        "restricted": true
    }
EOF
)

    bash $TEMPLATES/action-member-id "pri" "meeting" "add" "$JSON" $i
done
