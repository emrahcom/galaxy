#!/bin/bash
set -e

BASEDIR=$(dirname $0)
TEMPLATES=$BASEDIR/../../templates
source $BASEDIR/../../common

# ------------------------------------------------------------------------------
# try to add a new meeting for a nonexistent room
# ------------------------------------------------------------------------------
room_fake=$(uuidgen)
name="meeting-$(date +'%s')-$RANDOM"
JSON=$(cat <<EOF
{
    "room_id": "$room_fake",
    "name": "$name",
    "info": "some meeting info",
    "schedule_type": "permanent",
    "schedule_attr": "{}",
    "hidden": true,
    "restricted": false
}
EOF
)

bash $TEMPLATES/action-member-error "pri" "meeting" "add" "$JSON" 1
