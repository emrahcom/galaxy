#!/bin/bash
set -e

BASEDIR=$(dirname $0)
TEMPLATES=$BASEDIR/../../templates
source $BASEDIR/../../../config
source $BASEDIR/../../common

[[ -z "$1" ]] && SET=1 || SET=$1

# ------------------------------------------------------------------------------
# get member list (no limit, offset 1000000)
# ------------------------------------------------------------------------------
meeting_id=$(jq -r ".[0].id" $TMP/api-pri-meeting-add-id-$SET-1-res)
JSON=$(cat <<EOF
{
    "id": "$meeting_id",
    "offset": 1000000
}
EOF
)

bash $TEMPLATES/action-member-empty "$SET" "pri" "meeting/member" \
    "list/bymeeting" "$JSON" 1
