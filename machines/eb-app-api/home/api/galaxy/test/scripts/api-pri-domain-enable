#!/bin/bash
set -e

# ------------------------------------------------------------------------------
# environment
# ------------------------------------------------------------------------------
BASEDIR=$(dirname $0)
source $BASEDIR/common

header <<< "/api/pri/domain/enable"
rm -f /tmp/api-pri-domain-enable-*

# ------------------------------------------------------------------------------
# /api/pri/domain/enable (exists)
# ------------------------------------------------------------------------------
cookieJar=$(cat /tmp/kratos-login-cookie)
domainId=$(jq -r .id /tmp/api-pri-domain-add-0)

curl -ks -X POST https://$APP_FQDN/api/pri/domain/enable \
    --output-dir /tmp --output api-pri-domain-enable-0 \
    --cookie $cookieJar --cookie-jar $cookieJar \
    -H "Accept: application/json" \
    -H "Content-Type: application/json" \
    --data @- << EOF
    {
        "id": "$domainId"
    }
EOF
res=$(jq . /tmp/api-pri-domain-enable-0)
out <<< "exists:"
out <<< "$res"

[[ -z "$(echo $res | jq .)" ]] && exit 1
[[ "$(echo $res | jq -r .id)" = "null" ]] && exit 1

# ------------------------------------------------------------------------------
# /api/pri/domain/enable (not exists)
# ------------------------------------------------------------------------------
cookieJar=$(cat /tmp/kratos-login-cookie)
domainId=$(uuidgen)

curl -ks -X POST https://$APP_FQDN/api/pri/domain/enable \
    --output-dir /tmp --output api-pri-domain-enable-1 \
    --cookie $cookieJar --cookie-jar $cookieJar \
    -H "Accept: application/json" \
    -H "Content-Type: application/json" \
    --data @- << EOF
    {
        "id": "$domainId"
    }
EOF
res=$(jq . /tmp/api-pri-domain-enable-1)
out <<< "not exists:"
out <<< "$res"

[[ -n "$res" ]] && exit 1

# ------------------------------------------------------------------------------
# completed
# ------------------------------------------------------------------------------
footer <<< "/api/pri/domain/enable"
